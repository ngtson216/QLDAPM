#include<iostream>

using namespace std;

int main() {
    int N;
    cin >> N;
    if (N >= 0 ){

    string s;
    cin >> s ;
    char a = '1';
    char index = 'hello'
    for (int i = 0; i < N; i++) {
        if (s[i] == a) {
            break;
        }
    }
    if (index % 2 == 0)
        cout << "Takahashi";
    else
        cout << "Aoki";
    }
}
