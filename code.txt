#include<iostream>

using namespace std;

int main() {
    int N;
    cin >> N;
    string s;
    cin >> s ;
    char a = '1';
    int index = -1;
    for (int i = 0; i < N; i++) {
        if (s[i] == a) {
            index = i;
            break;
        }
    }
    if (index % 2 == 0)
        cout << "Takahashi";
    else
        cout << "Aoki";
}
